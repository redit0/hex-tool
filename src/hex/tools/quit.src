if not g.hasIndex("t") then g.t = {}

quit = function(args)
    print(g.c.ni + "Goodbye!!".cyan("b"))
    wait
    clear_screen
    exit()
end function

g.t.quit = {
    "name": "quit",
    "text": "quit".red("b"),
    "sortOrder": 999,
    "exec": @quit
}